# Public listening lab

The production listening lab is deployed at:

https://egor125552.github.io/rust-Aaja-game-engine-web/

The default test source is a real wooden-footstep loop. The native `Тестовый звук` select also exposes a real door-close transient and the synthetic diagnostic control.
